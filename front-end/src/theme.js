const { createTheme } = require("@mui/material");

export const darkTheme = createTheme({
  palette: {
    primary: {
      main: '#ffffff'
    },
    secondary: {
      main: "#edf2ff"
    },
    error: {
      main: "#edf2ff"
    }
  }
})